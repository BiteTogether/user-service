spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:database,security,jwt,openapi,firebase,kafka}
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 8MB
server:
  port: ${SERVER_PORT:8081}

logging:
  level:
    org.springframework.security: DEBUG
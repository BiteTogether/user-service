spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:database,security,jwt,openapi,firebase}
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
server:
  port: ${SERVER_PORT:8081}

logging:
  level:
    org.springframework.security: DEBUG